import Navbar from "@/components/header/navbar";

const Header = () => {
  return (
    <header className="flex flex-col justify-center items-center py-3 w-screen max-w-screen">
      <Navbar />
    </header>
  );
};

export default Header;
