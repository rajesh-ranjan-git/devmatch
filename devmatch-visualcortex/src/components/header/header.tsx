import Navbar from "@/components/header/navbar";

const Header = () => {
  return (
    <header className="flex justify-center items-center py-3 w-screen max-w-screen h-24 overflow-x-hidden">
      <Navbar />
    </header>
  );
};

export default Header;
